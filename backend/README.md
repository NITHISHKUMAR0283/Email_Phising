# PhishGuard AI Backend

- FastAPI app for phishing detection, explainability, quizzes, and logging.
- Modular structure: detection, explain, llm, rag, quiz, db

## Run
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
